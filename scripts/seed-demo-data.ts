/**
 * seed-demo-data.ts — Root-level convenience script to run the database seeder.
 *
 * Delegates to packages/db/src/seed.ts.
 * Run with: pnpm --filter @ada/db seed
 *           or:  tsx scripts/seed-demo-data.ts
 */
console.log('Run via: pnpm --filter @ada/db seed')
