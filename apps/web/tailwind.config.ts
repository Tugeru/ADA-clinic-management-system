/**
 * Tailwind CSS v4 — configuration is CSS-first.
 *
 * The actual Tailwind setup lives in:
 *   src/styles/tailwind.css  →  @import 'tailwindcss' + @source directives
 *
 * The @tailwindcss/vite plugin picks that up automatically via vite.config.ts.
 * This file is provided for IDE awareness / future customisation only and is
 * NOT required for the build to work.
 *
 * To add theme customisations in v4, use CSS @theme blocks inside
 * src/styles/theme.css rather than this config file.
 */
export default {}
