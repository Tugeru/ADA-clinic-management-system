
  # Patient Records and Inventory System

  This is a code bundle for Patient Records and Inventory System. The original project is available at https://www.figma.com/design/zi62kxev7o1VvIBXI2wJ7w/Patient-Records-and-Inventory-System.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  